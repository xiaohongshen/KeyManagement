<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="KeyManagment.Views.MainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+"
                     Clicked="OnNoteAddedClicked" />
    </ContentPage.ToolbarItems>
    <StackLayout Margin="20" x:Name="NoteViewStackLayout">
        <ListView x:Name="ListView"
              Margin="20"
              ItemSelected="OnListViewItemSelected">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text ="{Binding NameofApplication}"                          
                          Detail="{Binding PW}" />
            </DataTemplate>
        </ListView.ItemTemplate>
        </ListView>
        <Label TextColor = "White"
               BackgroundColor = "Green"
               LineBreakMode = "WordWrap" x:Name="InfoLabel">
            <Label.Text TextWrapping="Wrap" HorizontalOptions = "Center"  VerticalOptions="Center" >
                Welcome to the Key Management.&#x0a;
This page shows your application and its related password.
            </Label.Text>
        </Label>
    </StackLayout>

</ContentPage>
